server {
    # Coolify / Traefik default target is container port 80
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA shell — all routes fall back to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Same-origin API + WebSocket (works on https://your-domain)
    location /api/ {
        proxy_pass http://api:27341;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 7d;
        proxy_send_timeout 7d;
    }

    location = /health {
        proxy_pass http://api:27341/health;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }
}
